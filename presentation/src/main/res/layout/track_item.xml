<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="track"
            type="com.example.there.findclips.model.entity.spotify.Track" />
    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginLeft="3dp"
        android:layout_marginRight="3dp"
        android:layout_marginBottom="6dp"
        android:foreground="@drawable/spotify_foreground_ripple">

        <ImageView
            android:id="@+id/similar_track_cover_image_view"
            imageSrc="@{track.imageViewSrc}"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bottom_dark_gradient" />

        <TextView
            android:id="@+id/similar_track_name_txt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_margin="5dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="@{track.name}"
            android:textAlignment="center"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold" />

    </FrameLayout>
</layout>

