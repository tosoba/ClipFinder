<layout>

    <data>

        <import type="android.view.View" />

        <variable
            name="searchFragmentView"
            type="com.example.there.findclips.search.SearchFragmentView" />
    </data>

    <android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.example.there.findclips.search.SearchFragment">

        <android.support.design.widget.TabLayout
            android:id="@+id/spotify_tab_layout"
            onTabSelectedListener="@{searchFragmentView.onTabSelectedListener}"
            tabs="@{searchFragmentView.state.spotifyFragmentTabs}"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            android:minHeight="?attr/actionBarSize"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            android:visibility="@{searchFragmentView.state.spinnerSelection == 0 ? View.VISIBLE : View.GONE}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tabGravity="fill" />

        <SearchView
            android:id="@+id/search_view"
            onQueryTextListener="@{searchFragmentView.onQueryTextListener}"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:queryHint="@{searchFragmentView.state.queryHint}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/spotify_tab_layout" />

        <android.support.v4.view.ViewPager
            android:id="@+id/spotify_tab_view_pager"
            onPageChangeListener="@{searchFragmentView.onPageChangeListener}"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:adapter="@{searchFragmentView.pagerAdapter}"
            android:visibility="@{searchFragmentView.state.spinnerSelection == 0 ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/search_view" />

        <android.support.v7.widget.RecyclerView
            itemDecoration="@{searchFragmentView.videosItemDecoration}"
            layoutManager="@{searchFragmentView.videosLayoutManager}"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:adapter="@{searchFragmentView.videosAdapter}"
            android:visibility="@{searchFragmentView.state.spinnerSelection == 1 ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/search_view" />

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="@{(searchFragmentView.videosSearchViewState.videosLoadingInProgress &amp;&amp; searchFragmentView.state.spinnerSelection == 1) ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/search_view" />

    </android.support.constraint.ConstraintLayout>
</layout>